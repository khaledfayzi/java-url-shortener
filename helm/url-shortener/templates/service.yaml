apiVersion: v1
kind: Service
metadata:
  name: url-shortener
spec:
  type: {{ .Values.service.type }}
  selector:
    app: url-shortener
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
